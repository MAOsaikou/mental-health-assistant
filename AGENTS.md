## Agent skills

### Issue tracker

工单以 markdown 文件形式存放在 `.scratch/<feature>/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个分诊角色与标签一一对应：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：仓库根目录一份 `CONTEXT.md`，加上 `docs/adr/`。详见 `docs/agents/domain.md`。
